const chai = require('chai');
const assert = chai.assert;
const utils = require('../lib/utils');

suite('public machine ip address', function() {
  test('get current ip address', function(done) {
    done();
  });
});